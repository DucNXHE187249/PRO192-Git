import java.util.AbstractList;

public class OrderManagement {
    public static void main(String [] args){
        String [] options = {"Add new product", "Update price", "List all products", 
            "Create new order", "Print order by OrderID", "Sort ascending by price", 
            "Print order by CustomerID", "Export information by OrderID to a text file"};
        int choice = 0;
        Order list = new Order
        
    }
}
